# Tracker: always push to production

Owner preference (2026-07-25+): for the **Limitless Tracker** (`tracker/`), always commit on `develop`, merge/publish to `main`, and push so Netlify goes live. Do not leave tracker fixes only on `develop`/local.
