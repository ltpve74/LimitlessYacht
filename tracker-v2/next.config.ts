import type { NextConfig } from "next";

/**
 * v2 is a parallel commercial app. Do not rewrite URLs into live /tracker.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
